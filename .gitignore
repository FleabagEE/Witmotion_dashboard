.venv/
__pycache__/
*.pyc
.env
deploy/mosquitto/config/passwd

# The built dashboard. Generated by deploy/build-dashboard.sh into the
# directory the API serves from; see backend/routes/web.php.
#
# Not committed: it is minified output with content-hashed names, so every
# rebuild would rewrite the whole thing and bury the real diff. A fresh clone
# has no dashboard until it is built, and the appliance says so in plain words
# with a 503 rather than a blank page.
backend/public/index.html
backend/public/assets/
backend/public/favicon.svg
backend/public/icons.svg
